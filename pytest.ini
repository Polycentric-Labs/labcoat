[pytest]
# Pins the asyncio mode the suite's two @pytest.mark.asyncio tests rely on. Without pytest-asyncio installed
# (see requirements-dev.txt) those two tests FAIL with "async def functions are not natively supported".
asyncio_mode = strict
testpaths = tests

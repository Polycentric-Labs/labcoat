# Ranked synthesis — <TopicName>

> Phase 5 output. The deliverable. Ruthless ranking with per-item EVIDENCE + an honest SKIP/KILL list.
> Every proper noun here MUST trace to a `confirmed` row in the verification ledger (TRIPWIRE).

**Date:** <YYYY-MM-DD> · **Question:** <the real question from decomposition> · **Depth tier:** <tier> · **Actual cost:** <$X>

## Bottom line
<2–4 sentences: the answer. Lead with it.>

## Ranked findings / recommendations
> Ranked best-first. Each item carries the evidence that earned its rank (link the ledger rows).

### 1. <item> — <one-line verdict>
- **Why it ranks here:** <reasoning>
- **Evidence:** <verified facts; ledger rows #N, #M>
- **Survived kill attempt because:** <R-4 result>

### 2. <item> — <one-line verdict>
- **Why it ranks here:**
- **Evidence:**
- **Survived kill attempt because:**

### 3. <item>
- ...

## SKIP / KILL list (the honest part)
> What NOT to do, and why. As valuable as the ranking. "Already exists / saturated / no demand / unverifiable."
- **<item> — SKIP:** <reason; evidence>
- **<item> — KILL:** <reason; evidence (e.g. "already published — ledger row #K")>

## Open / unverifiable
<Anything that could not be confirmed against a primary source — explicitly labeled, never presented as fact.>

## Hand-off
- **Novelty log appended:** <domains added via novelty_log.append_good_sources>
- **research-resync stream registered:** <yes/no — per-project YAML path>
- **Memory pointer added (leaf-only):** <path — per-project pointer to this doc; NO global hub>
